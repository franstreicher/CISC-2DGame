extends Node2D

func _ready():
# Called when the node enters the scene tree for the first time.$AnimationPlayer.play("Fade in")
# Called every frame. 'delta' is the elapsed time since the previous frame.unc _process(delta):
	pass
